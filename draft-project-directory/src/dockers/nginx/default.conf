# This configuration is generated by the mozilla config generator

# generated 2026-08-06, TLSRef Guideline v6.0, nginx 1.27.3, OpenSSL 4.0.1, intermediate config, HSTS, gitrev=1b22dc6
# https://configurator.tlsref.org/#server=nginx&version=1.27.3&config=intermediate&openssl=4.0.1&hsts&guideline=6.0

server {
		# intermediate configuration
		ssl_protocols TLSv1.2 TLSv1.3;
		ssl_ecdh_curve X25519MLKEM768:X25519:prime256v1:secp384r1;
		ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
		ssl_prefer_server_ciphers off;

		# see also ssl_session_ticket_key alternative to stateful session cache
		ssl_session_timeout 1d;
		ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions

    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    ssl_certificate test;
    ssl_certificate_key /path/to/private_key;

    # HSTS (ngx_http_headers_module is required) (63072000 seconds)
    #add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;

		location / {
			return 404;
		}

}


## HSTS
#server {
#    listen 80 default_server;
#    listen [::]:80 default_server;

#    return 308 https://$host$request_uri;
#}

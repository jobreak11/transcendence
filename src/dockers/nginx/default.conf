# This configuration is generated by the mozilla config generator

# generated 2026-08-06, TLSRef Guideline v6.0, nginx 1.27.3, OpenSSL 4.0.1, intermediate config, HSTS, gitrev=1b22dc6
# https://configurator.tlsref.org/#server=nginx&version=1.27.3&config=intermediate&openssl=4.0.1&hsts&guideline=6.0

server {
		# intermediate configuration
		ssl_protocols TLSv1.2 TLSv1.3;
		ssl_ecdh_curve X25519MLKEM768:X25519:prime256v1:secp384r1;
		ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
		ssl_prefer_server_ciphers off;

		# see also ssl_session_ticket_key alternative to stateful session cache
		ssl_session_timeout 1d;
		ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions

    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name suttawit.russmp.duckdns.org;

    ssl_certificate test;
    ssl_certificate_key /path/to/private_key;

    # HSTS (ngx_http_headers_module is required) (63072000 seconds)
    #add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;

		location / {

      # Forward traffic to local Node.js server
      proxy_pass http://nextjs:3000/;

      # Proxy Headers (Tells Next.js original domain and HTTPS protocol)
      proxy_set_header Host $http_host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_http_version 1.1;  # CRITICAL: Enables WebSocket upgrading

      proxy_cache_bypass $http_upgrade;
		}

    location ~ ^/_next/(hmr|webpack-hmr) {
        proxy_pass http://nextjs:3000/;
        proxy_http_version 1.1;  # CRITICAL: Enables WebSocket upgrading
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;

        # Prevent websocket connection drops during idle dev sessions
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location /nestjs {
      rewrite ^/nestjs/(.*) /$1 break;
      rewrite ^/nestjs$ / break;

      proxy_pass http://nestjs:3001;
      proxy_http_version 1.1;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_cache_bypass $http_upgrade;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }

    location /adminer {
      rewrite ^/adminer/(.*) /$1 break;
      rewrite ^/adminer$ / break;

      proxy_pass http://adminer:8080;
      proxy_http_version 1.1;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_cache_bypass $http_upgrade;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }


    location /dozzle {
      #rewrite ^/dozzle/(.*) /$1 break;
      #rewrite ^/dozzle$ / break;

      proxy_pass http://dozzle:8888;
      proxy_http_version 1.1;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_cache_bypass $http_upgrade;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }


    location /redis-insight {
      #rewrite ^/redis-insight/(.*) /$1 break;
      #rewrite ^/redis-insight$ / break;

      proxy_pass http://redis-commander:5540;
      proxy_http_version 1.1;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_cache_bypass $http_upgrade;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }

}


## HSTS
#server {
#    listen 80 default_server;
#    listen [::]:80 default_server;

#    return 308 https://$host$request_uri;
#}
